# setup.py
from setuptools import setup, find_packages

setup(
    name='lvp',  # Name of your project/package
    version='0.1',
    packages=find_packages(),  # Automatically discover all packages in the directory
)